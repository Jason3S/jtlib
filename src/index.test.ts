/**
 * General test to bring things together
 */

import * as chai from 'chai';
import * as jtl from './index';

const expect = chai.expect;

/*
describe('Top Level Library Test', () => {
    it('Tests a basic transform', () => {
        const def: jtl.TransformationDefinition = {
            toc: jtl.field('table_of_contents').transform( {
                title: jtl.field('title'),
                bookTitle: jtl.root.field('title')

            })
        };
    });
});

*/
